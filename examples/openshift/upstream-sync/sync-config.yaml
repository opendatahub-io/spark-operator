upstream:
  repo: https://github.com/kubeflow/spark-operator
  branch: master

midstream:
  branch: main

# Strictly midstream-only files — do not exist in upstream.
# Auto-resolved silently on conflict. No review needed.
safe-ignore:
  - ".tekton/*"
  - "examples/openshift/**"
  - "spark-operator-module/**"
  - "Makefile.midstream*.mk"
  - "Makefile.spark-operator-module.mk"
  - "test/upgrade/**"
  - "chaos/**"
  - "spark-operator-module-controller.Dockerfile"
  - ".github/workflows/upstream-sync.yaml"

# Files that exist in BOTH upstream and midstream with different content.
# Auto-resolved with ours on conflict, but flagged in the PR body
# with a diff of what upstream changed so a human can review.
risky-ignore:
  - "Makefile"
  - "config/default/kustomization.yaml"
  - "config/default/params.env"
  - "config/manager/manager.yaml"
  - "config/rbac/role.yaml"
  - "config/webhook/deployment.yaml"
  - "config/overlays/**"
  - "config/pdb/**"
  - "config/spark-rbac/**"
  - "cmd/operator/webhook/start.go"
  - "test/drift/drift_test.go"
  - "test/e2e/suite_test.go"
  - "test/kustomize/**"
  - "docs/website/**"
  - "api/python_api/**"
  - "api/openapi-spec/**"
  - "hack/openapi/**"
  - "hack/python-api/**"
  - "hack/swagger/**"
  - "pkg/tls/**"
  - "pkg/version/**"

# CI workflow files — exist in upstream, midstream adds branch triggers.
# Auto-resolved with ours on conflict, flagged for review.
ci-ignore:
  - ".github/workflows/integration.yaml"
  - ".github/workflows/release.yaml"
  - ".github/workflows/docs.yaml"
  - ".github/workflows/kustomize-drift-check.yaml"
  - ".github/workflows/kustomize-lint.yaml"
  - ".github/workflows/shell-lint.yaml"

# Post-merge validation commands (run in order, all must pass).
validation:
  - "go mod tidy"
  - "make build-operator"
