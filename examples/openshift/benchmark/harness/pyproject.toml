[project]
name = "openshift-spark-operator-benchmark"
version = "0.1.0"
description = "OpenShift AI adaptation of the Kubeflow Spark Operator Locust benchmark harness"
requires-python = ">=3.11"
dependencies = [
  "kubernetes>=29.0.0",
  "locust==2.23.1",
  "pyyaml>=6.0.1",
  "python-dateutil>=2.9.0",
]

[project.optional-dependencies]
dev = []

[tool.uv]
package = false
